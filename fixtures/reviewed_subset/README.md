# Reviewed Subset

This folder keeps the small manually spot-checked metadata subset that underpins the demo batch.

Contents:

- `manifest.csv`
- `manifest.json`

Important notes:

- these rows are still derived from approved public record content
- they are not original internal TTB application exports
- the larger clean-pair corpus is intentionally not carried into this migrated root

Why it is still kept:

- it documents the reviewed source examples behind the demo batch
- it preserves the higher-confidence metadata subset without carrying the full historical corpus
